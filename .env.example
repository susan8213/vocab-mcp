# Google Gemini API Key (Required)
# 從 https://aistudio.google.com/apikey 取得
GEMINI_API_KEY=your_api_key_here

# 模型設定 (Optional, 預設均為 gemini-2.5-flash)
GEMINI_EXPAND_MODEL=gemini-2.5-flash
GEMINI_EXTRACT_MODEL=gemini-2.5-flash

# 輸出限制 (Optional)
EXPAND_MAX_TOKENS=4096
EXTRACT_MAX_TOKENS=8192

# Debug mode (Optional)
# 設為 1 可在 stderr 看到 Gemini prompt 與 raw response
DEBUG=0
